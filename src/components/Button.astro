---
const { onClick, children, class: className } = Astro.props;

console.log(children); // Asegúrate de que children esté siendo pasado correctamente
---

<button
  class={`px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 ${className}`}
  onclick={onClick}
>
  {children}
</button>
